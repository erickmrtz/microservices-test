server:
  # port: 8080
  port: 8091
  
spring:
  application:
    name: product-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
      # defaultZone: http://10.0.0.129:8080/eureka/
